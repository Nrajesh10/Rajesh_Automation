### test_ai_code.py
```python
import pytest
from ai_code import multiply_numbers

def test_multiply_numbers():
    assert multiply_numbers(2, 3) == 6
    assert multiply_numbers(-1, 1) == -1
    assert multiply_numbers(0, 5) == 0
    assert multiply_numbers(2.5, 4) == 10.0

# Added a final newline to satisfy PEP 8
```