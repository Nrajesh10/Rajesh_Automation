### test_ai_code.py
```python
import pytest
from ai_code import multiply_numbers

def test_multiply_numbers():
    assert multiply_numbers(2, 3) == 6
    assert multiply_numbers(-1, 5) == -5
    assert multiply_numbers(0, 10) == 0
    assert multiply_numbers(4.5, 2) == 9.0
``` 

Make sure that you remove the ```python markers and any surrounding markdown in your test file to avoid the SyntaxError during the import.